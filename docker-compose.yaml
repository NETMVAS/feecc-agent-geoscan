version: "3"

volumes:
  agent-storage:

services:
  robonomics_qa_agent:
    build: .
    ports:
      - 5000:5000
    environment:
      - TZ=Europe/Moscow
    devices:
      - /dev/bus/usb:/dev/bus/usb